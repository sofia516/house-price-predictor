# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# --- Outputs (regenerated by running the script) ---
# Keep the small CSVs and the EDA/pred-vs-actual PNGs as project artifacts,
# but skip the heavyweight serialized model.
# (Actually keep them — Streamlit Cloud needs them to run the app.)
# artifacts/*.joblib
# *.joblib

# --- Local secrets / env ---
.env
.env.local
